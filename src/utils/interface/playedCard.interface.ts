interface IPlayedCard {
  readonly img: string; // player's photo
  readonly name: string; // player's name
  readonly money: number; // total money
  readonly color: string; // player's color
  readonly indexUnic: number; // key
}
